<%= form_with(model: surver, local: true) do |form| %>
  <% if surver.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(surver.errors.count, "error") %> prohibited this surver from being saved:</h2>

      <ul>
      <% surver.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
